"""Agent-layer foundations: command parsing and the tool-safety registry.

Phase 4 (agents & tools). These modules are framework-agnostic; concrete agent
runners (Microsoft Agent Framework, Foundry toolbox, MCP) plug in on top.
"""
