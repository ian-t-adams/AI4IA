"""Default-off application admission, not a cap on the Azure bill.

Durable activation and bootstrap are deliberately unavailable. The executable
store contract operates only on explicitly seeded, owner-scoped state.
"""
