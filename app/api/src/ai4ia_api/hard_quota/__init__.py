"""Default-off application admission, not a cap on the Azure bill.

Outside the explicitly seeded local test fake, durable coordination exists only
for an operator-approved request-count rollout (``factory``). Owner documents
come only from the digest-approved, create-only operator bootstrap (``operator``).
"""
